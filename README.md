
---

## 📁 `mod/README.md`


This project benchmarks an optimized x86 (IA-32) assembly program performing modular arithmetic on input data, with execution time measured in CPU cycles using RDTSC.

### Build & Run
```bash
make            # uses rand1M by default
make INPUT=file # run with a custom input file
